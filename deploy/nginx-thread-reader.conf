# 先用这个版本跑通公网 IP 访问。
# 它默认只启用 HTTP，不引用任何尚未签发的证书文件。
#
# 使用方式：
# 1. 把下面的 123.57.29.76 改成你的公网 IP 或域名
# 2. 复制到 /etc/nginx/sites-available/thread-reader
# 3. sudo nginx -t && sudo systemctl reload nginx
#
# 等你后面真的拿到证书后，再启用文末的 HTTPS server 块。

server {
    listen 80;
    listen [::]:80;
    server_name 123.57.29.76;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto http;
        proxy_read_timeout 300;
        proxy_send_timeout 300;
    }
}

# 下面是 HTTPS 模板。
# 只有在这些文件已经存在时，才能取消注释启用：
# /etc/letsencrypt/live/123.57.29.76/fullchain.pem
# /etc/letsencrypt/live/123.57.29.76/privkey.pem
#
# server {
#     listen 80;
#     listen [::]:80;
#     server_name 123.57.29.76;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name 123.57.29.76;
#
#     ssl_certificate /etc/letsencrypt/live/123.57.29.76/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/123.57.29.76/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers off;
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:10m;
#
#     client_max_body_size 20m;
#
#     add_header Strict-Transport-Security "max-age=31536000" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 300;
#         proxy_send_timeout 300;
#     }
# }
